

## Fix "Total" KPI on Incidents Card

Change the KPI label from "Total" to "Total Reported" so it's clear this number reflects incidents the user personally reported.

### File to modify
- `src/components/action-center/modules/IncidentsModule.tsx` — Update the label and translation key for the Total KPI from `'Total'` to `'Total Reported'`.

