import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

interface LicensedUserQuotaCardProps {
  quota?: any;
  isLoading?: boolean;
}

export function LicensedUserQuotaCard({ quota, isLoading }: LicensedUserQuotaCardProps) {
  return null; // Stub component
}
